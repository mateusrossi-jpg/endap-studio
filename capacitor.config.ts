import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.endap.studio',
  appName: 'ENDAP Studio',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
