---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x543c2ab
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x543c2ab.md
---

# `unknown_0x543c2ab` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x543c2ab–0x543c39f

| field | flag | tag |
|---|---|---|
| `Workspace` | 0x3 | `jsonschema_description:"Workspace mode for the subagent. 'inherit' (default) shares the parent's workspace. 'branch' creates a new workspace branched from the parent (CitC clone or git worktree). If omitted, defaults to 'inherit'."` |

### Parsed tags

**`Workspace`**
- `jsonschema_description` = `Workspace mode for the subagent. 'inherit' (default) shares the parent's workspace. 'branch' creates a new workspace branched from the parent (CitC clone or git worktree). If omitted, defaults to 'inherit'.`

