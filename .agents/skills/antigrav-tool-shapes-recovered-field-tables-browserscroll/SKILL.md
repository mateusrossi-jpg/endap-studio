---
name: antigrav-tool-shapes-recovered-field-tables-browserscroll
description: Antigravity prompt from tool_shapes/recovered_field_tables/BrowserScroll.md
---

# `BrowserScroll` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x5004a51–0x5004ab8
- Likely cortex step: `CortexStepBrowserScroll`

| field | flag | tag |
|---|---|---|
| `ClearText` | 0x3 | `jsonschema_description:"Whether to clear existing text before inputting. Default is false."` |

### Parsed tags

**`ClearText`**
- `jsonschema_description` = `Whether to clear existing text before inputting. Default is false.`

## Cluster @ 0x502bf19–0x502bf85
- Likely cortex step: `CortexStepBrowserScroll`

| field | flag | tag |
|---|---|---|
| `IsArtifact` | 0x3 | `jsonschema:"required" jsonschema_description:"Set this to true when creating an artifact file."` |

### Parsed tags

**`IsArtifact`**
- `jsonschema` = `required`
- `jsonschema_description` = `Set this to true when creating an artifact file.`

