---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x537940a
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x537940a.md
---

# `unknown_0x537940a` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x537940a–0x53794dc

| field | flag | tag |
|---|---|---|
| `Options` | 0x3 | `json:"options" jsonschema_description:"The text for each option, formatted as the user's response. Must have at least 2 options. Do NOT add an 'Other' option to questions." jsonschema_required:"true"` |

### Parsed tags

**`Options`**
- `json` = `options`
- `jsonschema_description` = `The text for each option, formatted as the user's response. Must have at least 2 options. Do NOT add an 'Other' option to questions.`
- `jsonschema_required` = `true`

