---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x550c221
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x550c221.md
---

# `unknown_0x550c221` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x550c221–0x550c3be

| field | flag | tag |
|---|---|---|
| `Model` | 0x3 | `jsonschema_description:"Model to use for the subagent. 'inherit' (default) uses the calling agent's model. 'fast' uses a smaller, faster model suited for simple tasks like research lookups, file reading, or quick searches. 'heavy' uses a larger, more capable model suited for complex tasks requiring deep reasoning, large refactors, or multi-step planning." jsonschema:"enum=inherit,enum=fast,enum=heavy"` |

### Parsed tags

**`Model`**
- `jsonschema_description` = `Model to use for the subagent. 'inherit' (default) uses the calling agent's model. 'fast' uses a smaller, faster model suited for simple tasks like research lookups, file reading, or quick searches. 'heavy' uses a larger, more capable model suited for complex tasks requiring deep reasoning, large refactors, or multi-step planning.`
- `jsonschema` = `enum=inherit,enum=fast,enum=heavy`

