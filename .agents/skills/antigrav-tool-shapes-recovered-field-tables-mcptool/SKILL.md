---
name: antigrav-tool-shapes-recovered-field-tables-mcptool
description: Antigravity prompt from tool_shapes/recovered_field_tables/McpTool.md
---

# `McpTool` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x4ff333a–0x4ff339c
- Likely cortex step: `CortexStepMcpTool`

| field | flag | tag |
|---|---|---|
| `Domain` | 0x3 | `json:"domain" jsonschema_description:"Optional domain to recommend the search prioritize"` |

### Parsed tags

**`Domain`**
- `json` = `domain`
- `jsonschema_description` = `Optional domain to recommend the search prioritize`

## Cluster @ 0x51dde2d–0x51ddec0
- Likely cortex step: `CortexStepMcpTool`

| field | flag | tag |
|---|---|---|
| `StartLine` | 0x3 | `jsonschema_description:"Optional. Startline to view, 1-indexed as usual, inclusive. This value must be less than or equal to EndLine."` |

### Parsed tags

**`StartLine`**
- `jsonschema_description` = `Optional. Startline to view, 1-indexed as usual, inclusive. This value must be less than or equal to EndLine.`

