---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x54f32ee
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x54f32ee.md
---

# `unknown_0x54f32ee` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x54f32ee–0x54f3464

| field | flag | tag |
|---|---|---|
| `ScrollByElementIndex` | 0x3 | `jsonschema_description:"if true, scroll by the element with the given index; the scroll is performed via executing a mouseWheel event on the pixel at the middle of the element.. Otherwise scroll the entire page; in this case, if 0 pixels are scrolled, the page is likely not scrollable and the tool call should be retried by scrolling a DOM element."` |

### Parsed tags

**`ScrollByElementIndex`**
- `jsonschema_description` = `if true, scroll by the element with the given index; the scroll is performed via executing a mouseWheel event on the pixel at the middle of the element.. Otherwise scroll the entire page; in this case, if 0 pixels are scrolled, the page is likely not scrollable and the tool call should be retried by scrolling a DOM element.`

