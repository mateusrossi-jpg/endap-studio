---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x52b36fc
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x52b36fc.md
---

# `unknown_0x52b36fc` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x52b36fc–0x52b3854

| field | flag | tag |
|---|---|---|
| `X` | 0x3 | `jsonschema:"required" jsonschema_description:"X coordinate of the pixel to scroll (0-999). Coordinates are scaled to a 1000x1000 grid and mapped to screen dimensions."` |
| `Y` | 0x3 | `jsonschema:"required" jsonschema_description:"Y coordinate of the pixel to scroll (0-999). Coordinates are scaled to a 1000x1000 grid and mapped to screen dimensions."` |

### Parsed tags

**`X`**
- `jsonschema` = `required`
- `jsonschema_description` = `X coordinate of the pixel to scroll (0-999). Coordinates are scaled to a 1000x1000 grid and mapped to screen dimensions.`

**`Y`**
- `jsonschema` = `required`
- `jsonschema_description` = `Y coordinate of the pixel to scroll (0-999). Coordinates are scaled to a 1000x1000 grid and mapped to screen dimensions.`

