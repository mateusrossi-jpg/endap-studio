---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x547a7d0
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x547a7d0.md
---

# `unknown_0x547a7d0` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x547a7d0–0x547a8d9

| field | flag | tag |
|---|---|---|
| `TaskSummary` | 0x3 | `jsonschema:"required" jsonschema_description:"A short, user-friendly summary of the task (1-2 sentences max). This will be displayed to the user in the UI instead of the full task description. Should be concise and describe the goal at a high level."` |

### Parsed tags

**`TaskSummary`**
- `jsonschema` = `required`
- `jsonschema_description` = `A short, user-friendly summary of the task (1-2 sentences max). This will be displayed to the user in the UI instead of the full task description. Should be concise and describe the goal at a high level.`

