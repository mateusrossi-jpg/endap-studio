---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x5365878
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x5365878.md
---

# `unknown_0x5365878` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x5365878–0x5365a08

| field | flag | tag |
|---|---|---|
| `Y` | 0x3 | `jsonschema:"required" jsonschema_description:"Y coordinate of the pixel to click (0-999). Coordinates are scaled to a 1000x1000 grid and mapped to screen dimensions when executing the tool call."` |
| `X` | 0x3 | `jsonschema:"required" jsonschema_description:"X coordinate of the pixel to click (0-999). Coordinates are scaled to a 1000x1000 grid and mapped to screen dimensions when executing the tool call."` |

### Parsed tags

**`Y`**
- `jsonschema` = `required`
- `jsonschema_description` = `Y coordinate of the pixel to click (0-999). Coordinates are scaled to a 1000x1000 grid and mapped to screen dimensions when executing the tool call.`

**`X`**
- `jsonschema` = `required`
- `jsonschema_description` = `X coordinate of the pixel to click (0-999). Coordinates are scaled to a 1000x1000 grid and mapped to screen dimensions when executing the tool call.`

