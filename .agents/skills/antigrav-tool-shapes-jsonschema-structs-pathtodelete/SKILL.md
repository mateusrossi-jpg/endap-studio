---
name: antigrav-tool-shapes-jsonschema-structs-pathtodelete
description: Antigravity prompt from tool_shapes/jsonschema_structs/pathtodelete.md
---

# `ParseToolArgs` struct @ 0x2f97f7a

Recovered byte-exact from a Go generic instantiation symbol in the
language-server binary. This is the JSON-schema-source struct passed
to `utils.ToJsonSchemaString` / `utils.ParseToolArgs`.

## Fields

- `PathToDelete` _string_
  - `jsonschema_description` = `Absolute path to the file or directory to delete. Must be either within an artifacts/ subdirectory of a Knowledge Item, or a top-level Knowledge Item directory.`

## Raw body

```
 PathToDelete string "jsonschema_description:\"Absolute path to the file or directory to delete. Must be either within an artifacts/ subdirectory of a Knowledge Item, or a top-level Knowledge Item directory.\"" 
```

