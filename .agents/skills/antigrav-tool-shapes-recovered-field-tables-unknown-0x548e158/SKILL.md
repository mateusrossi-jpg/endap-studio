---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x548e158
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x548e158.md
---

# `unknown_0x548e158` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x548e158–0x548e386

| field | flag | tag |
|---|---|---|
| `CaptureBeyondViewport` | 0x3 | `jsonschema_description:"If true, captures an extended screenshot starting from the current scroll position downward, up to 4000px or the end of page content, whichever is less. To capture content above or below this range, scroll first and then capture."` |
| `Importance` | 0x3 | `jsonschema_description:"A measure of how important and relevant the edit is to the user's task. Use 'high' for edits directly addressing the main request or fixing critical issues, 'medium' for supporting changes, 'low' for minor improvements. enum=high,medium,low"` |

### Parsed tags

**`CaptureBeyondViewport`**
- `jsonschema_description` = `If true, captures an extended screenshot starting from the current scroll position downward, up to 4000px or the end of page content, whichever is less. To capture content above or below this range, scroll first and then capture.`

**`Importance`**
- `jsonschema_description` = `A measure of how important and relevant the edit is to the user's task. Use 'high' for edits directly addressing the main request or fixing critical issues, 'medium' for supporting changes, 'low' for minor improvements. enum=high,medium,low`

