---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x548030f
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x548030f.md
---

# `unknown_0x548030f` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x548030f–0x548041e

| field | flag | tag |
|---|---|---|
| `MatchPerLine` | 0x3 | `jsonschema_description:"If true, returns each line that matches the query, including line numbers and snippets of matching lines (equivalent to 'git grep -nI'). If false, only returns the names of files containing the query (equivalent to 'git grep -l')."` |

### Parsed tags

**`MatchPerLine`**
- `jsonschema_description` = `If true, returns each line that matches the query, including line numbers and snippets of matching lines (equivalent to 'git grep -nI'). If false, only returns the names of files containing the query (equivalent to 'git grep -l').`

