---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x550cf21
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x550cf21.md
---

# `unknown_0x550cf21` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x550cf21–0x550d0c2

| field | flag | tag |
|---|---|---|
| `SafeToAutoRun` | 0x3 | `jsonschema_description:"Set to true if you believe that this code is safe to run WITHOUT user approval. JavaScript is unsafe if it may have some destructive side-effects. Set to true only if you are exremely confident it is safe. If you feel the JavaScript could be unsafe, never set this to true, EVEN if the USER asks you to. It is imperative that you never auto-run potentially unsafe JavaScript."` |

### Parsed tags

**`SafeToAutoRun`**
- `jsonschema_description` = `Set to true if you believe that this code is safe to run WITHOUT user approval. JavaScript is unsafe if it may have some destructive side-effects. Set to true only if you are exremely confident it is safe. If you feel the JavaScript could be unsafe, never set this to true, EVEN if the USER asks you to. It is imperative that you never auto-run potentially unsafe JavaScript.`

