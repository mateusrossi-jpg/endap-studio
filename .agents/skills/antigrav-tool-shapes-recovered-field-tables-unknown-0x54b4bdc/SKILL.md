---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x54b4bdc
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x54b4bdc.md
---

# `unknown_0x54b4bdc` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x54b4bdc–0x54b4d07

| field | flag | tag |
|---|---|---|
| `Role` | 0x3 | `jsonschema:"required" jsonschema_description:"A 2-5 word description of the subagent's role. Should read similar to a job title, e.g. 'Codebase Researcher', 'Database Debugger', etc. Should also be detailed enough to distinguish between different subagents who might share similar purposes."` |

### Parsed tags

**`Role`**
- `jsonschema` = `required`
- `jsonschema_description` = `A 2-5 word description of the subagent's role. Should read similar to a job title, e.g. 'Codebase Researcher', 'Database Debugger', etc. Should also be detailed enough to distinguish between different subagents who might share similar purposes.`

