---
name: antigrav-tool-shapes-recovered-field-tables-capturebrowserconsolelogs
description: Antigravity prompt from tool_shapes/recovered_field_tables/CaptureBrowserConsoleLogs.md
---

# `CaptureBrowserConsoleLogs` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x5052f1b–0x5052f8e
- Likely cortex step: `CortexStepCaptureBrowserConsoleLogs`

| field | flag | tag |
|---|---|---|
| `References` | 0x3 | `jsonschema_description:"List of references related to this Knowledge Item" json:"references,omitempty"` |

### Parsed tags

**`References`**
- `jsonschema_description` = `List of references related to this Knowledge Item`
- `json` = `references,omitempty`

## Cluster @ 0x505347f–0x50534f2
- Likely cortex step: `CortexStepCaptureBrowserConsoleLogs`

| field | flag | tag |
|---|---|---|
| `Value` | 0x3 | `jsonschema:"required" jsonschema_description:"The value or text of the option to select from the dropdown."` |

### Parsed tags

**`Value`**
- `jsonschema` = `required`
- `jsonschema_description` = `The value or text of the option to select from the dropdown.`

## Cluster @ 0x50c27a7–0x50c289b
- Likely cortex step: `CortexStepCaptureBrowserConsoleLogs`

| field | flag | tag |
|---|---|---|
| `Button` | 0x3 | `jsonschema:"required" jsonschema_description:"Mouse button to release. Options are 'left', 'right', or 'middle'."` |
| `Index` | 0x3 | `jsonschema:"required" jsonschema_description:"Index of the annotated DOM select element to select an option from."` |

### Parsed tags

**`Button`**
- `jsonschema` = `required`
- `jsonschema_description` = `Mouse button to release. Options are 'left', 'right', or 'middle'.`

**`Index`**
- `jsonschema` = `required`
- `jsonschema_description` = `Index of the annotated DOM select element to select an option from.`

## Cluster @ 0x50c2d5f–0x50c2dd9
- Likely cortex step: `CortexStepCaptureBrowserConsoleLogs`

| field | flag | tag |
|---|---|---|
| `Instruction` | 0x3 | `jsonschema:"required" jsonschema_description:"A description of the changes that you are making to the file."` |

### Parsed tags

**`Instruction`**
- `jsonschema` = `required`
- `jsonschema_description` = `A description of the changes that you are making to the file.`

