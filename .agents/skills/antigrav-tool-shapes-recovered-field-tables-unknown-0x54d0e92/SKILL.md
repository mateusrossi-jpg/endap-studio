---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x54d0e92
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x54d0e92.md
---

# `unknown_0x54d0e92` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x54d0e92–0x54d0fe1

| field | flag | tag |
|---|---|---|
| `TargetContent` | 0x3 | `jsonschema:"required" jsonschema_description:"The exact string to be replaced. This must be the exact character-sequence to be replaced, including whitespace. Be very careful to include any leading whitespace otherwise this will not work at all. This must be a unique substring within the file, or else it will error."` |

### Parsed tags

**`TargetContent`**
- `jsonschema` = `required`
- `jsonschema_description` = `The exact string to be replaced. This must be the exact character-sequence to be replaced, including whitespace. Be very careful to include any leading whitespace otherwise this will not work at all. This must be a unique substring within the file, or else it will error.`

