---
name: antigrav-tool-shapes-recovered-field-tables-ephemeralmessage
description: Antigravity prompt from tool_shapes/recovered_field_tables/EphemeralMessage.md
---

# `EphemeralMessage` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x505d18d–0x505d277
- Likely cortex step: `CortexStepEphemeralMessage`

| field | flag | tag |
|---|---|---|
| `PageId` | 0x3 | `jsonschema:"required" jsonschema_description:"The page_id of the browser page to list network requests for."` |
| `Direction` | 0x3 | `jsonschema:"required" jsonschema_description:"direction of the scroll. Options are left, right, up, down"` |

### Parsed tags

**`PageId`**
- `jsonschema` = `required`
- `jsonschema_description` = `The page_id of the browser page to list network requests for.`

**`Direction`**
- `jsonschema` = `required`
- `jsonschema_description` = `direction of the scroll. Options are left, right, up, down`

