---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x537c257
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x537c257.md
---

# `unknown_0x537c257` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x537c257–0x537c32c

| field | flag | tag |
|---|---|---|
| `ReplacementChunks` | 0x3 | `jsonschema:"required" jsonschema_description:"A list of chunks to replace. It is best to provide multiple chunks for non-contiguous edits if possible. This must be a JSON array, not a string."` |

### Parsed tags

**`ReplacementChunks`**
- `jsonschema` = `required`
- `jsonschema_description` = `A list of chunks to replace. It is best to provide multiple chunks for non-contiguous edits if possible. This must be a JSON array, not a string.`

