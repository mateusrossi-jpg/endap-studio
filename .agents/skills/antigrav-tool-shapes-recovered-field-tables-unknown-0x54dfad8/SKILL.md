---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x54dfad8
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x54dfad8.md
---

# `unknown_0x54dfad8` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x54dfad8–0x54dfc28

| field | flag | tag |
|---|---|---|
| `IsRegex` | 0x3 | `jsonschema_description:"If true, treats Query as a regular expression pattern with special characters like *, +, (, etc. having regex meaning. If false, treats Query as a literal string where all characters are matched exactly. Use false for normal text searches and true only when you specifically need regex functionality."` |

### Parsed tags

**`IsRegex`**
- `jsonschema_description` = `If true, treats Query as a regular expression pattern with special characters like *, +, (, etc. having regex meaning. If false, treats Query as a literal string where all characters are matched exactly. Use false for normal text searches and true only when you specifically need regex functionality.`

