---
name: antigrav-tool-shapes-recovered-field-tables-generatormetadata
description: Antigravity prompt from tool_shapes/recovered_field_tables/GeneratorMetadata.md
---

# `GeneratorMetadata` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x4f911b9–0x4f91207
- Likely cortex step: `CortexStepGeneratorMetadata`

| field | flag | tag |
|---|---|---|
| `Type` | 0x3 | `jsonschema_description:"Optional, type filter, enum=file,directory,any"` |

### Parsed tags

**`Type`**
- `jsonschema_description` = `Optional, type filter, enum=file,directory,any`

## Cluster @ 0x513babc–0x513bb3d
- Likely cortex step: `CortexStepGeneratorMetadata`

| field | flag | tag |
|---|---|---|
| `Width` | 0x3 | `jsonschema_description:"The window contents width in display independent pixels. Only used when WindowState is 'normal'."` |

### Parsed tags

**`Width`**
- `jsonschema_description` = `The window contents width in display independent pixels. Only used when WindowState is 'normal'.`

## Cluster @ 0x517b8be–0x517b94b
- Likely cortex step: `CortexStepGeneratorMetadata`

| field | flag | tag |
|---|---|---|
| `OutputCharacterCount` | 0x3 | `jsonschema_description:"Number of characters to view. Make this as small as possible to avoid excessive memory usage."` |

### Parsed tags

**`OutputCharacterCount`**
- `jsonschema_description` = `Number of characters to view. Make this as small as possible to avoid excessive memory usage.`

