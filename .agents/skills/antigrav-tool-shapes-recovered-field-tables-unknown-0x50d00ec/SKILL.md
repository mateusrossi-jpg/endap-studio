---
name: antigrav-tool-shapes-recovered-field-tables-unknown-0x50d00ec
description: Antigravity prompt from tool_shapes/recovered_field_tables/unknown_0x50d00ec.md
---

# `unknown_0x50d00ec` recovered field table

Recovered from Go reflect-name records in language-server binary.

## Cluster @ 0x50d00ec–0x50d01e4

| field | flag | tag |
|---|---|---|
| `Terminate` | 0x3 | `jsonschema_description:"Whether to terminate the command. Exactly one of input and terminate must be specified."` |
| `Input` | 0x3 | `json:"Input,omitempty" jsonschema_description:"The input to send to the task. Required when Action is 'send_input'."` |

### Parsed tags

**`Terminate`**
- `jsonschema_description` = `Whether to terminate the command. Exactly one of input and terminate must be specified.`

**`Input`**
- `json` = `Input,omitempty`
- `jsonschema_description` = `The input to send to the task. Required when Action is 'send_input'.`

