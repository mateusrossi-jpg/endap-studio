class PortLayoutTheme {
  final double trackOffset; // Deslocamento das trilhas (ex: 25.0)
  final double pinRadius;   // Raio do pino (ex: 4.0)
  final double defaultHeight; // Altura padrão do bloco (ex: 100.0)

  const PortLayoutTheme({
    this.trackOffset = 25.0,
    this.pinRadius = 4.0,
    this.defaultHeight = 100.0,
  });
}
